{
  "name": "ai-bappa-maza",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "npx nx run-many --target=dev --projects=web,api --parallel",
    "dev:frontend": "npx nx dev web",
    "dev:backend": "npx nx dev api",
    "build": "npx nx build web",
    "start": "npx nx start api",
    "start:pm2": "npm run build && pm2 start ecosystem.config.js",
    "graph": "npx nx graph",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "nx": "^21.2.3"
  }
}